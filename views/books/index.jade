extends ../layout

block content
  include ../partials/left_nav
  .row
    include ../partials/search_nav
    .col-lg-9
      .row.my-4
        each book in data
          .col-lg-3.col-md-4.mb-4
            .card.h-100
              a(href='#')
                img.card-img-top(src='/images/drg-big@2x.png', alt='')
              .card-body
                h5.card-title
                  a(href='#', data-toggle='modal', data-target='#SoftwareDefinedNetwork') #{book.name}
                h6 Rs 1565
                p.card-text #{book.description}
              .card-footer
                small.text-muted ★ ★ ★ ★ ★
        // /.row
    // /.col-lg-9
  // /.row
// /.container
